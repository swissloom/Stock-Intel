[build]
builder = "NIXPACKS"
buildCommand = "cd server && pip install -r requirements.txt"

[deploy]
startCommand = "cd server && python3 server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
